# React, Vite, and TypeScript Host Renderer

The Electron host renderer will use React, Vite, and TypeScript. This matches the bundled-plugin implementation stack and enables selective reuse of TractIt's shell patterns while preserving the rule that host UI does not render plugin business content.
