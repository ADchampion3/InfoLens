# Manual-Only Default Refresh

Newly installed plugins will default to manual-only refresh. Automatic background collection begins only after the user explicitly selects a fixed interval in the plugin's own settings, preventing unexpected browser activity and resource use on first installation.
