# Discovery-Time Package Compatibility

The host will apply package-contract, host-version, OpenCLI-version-range, strategy, and command-availability checks both before installation and during every startup discovery. A rejected package is not activated or shown in ordinary navigation, but remains visible in plugin management with its reason and can be removed. A compatible package with no existing host state is enabled by default, and successful installation enables the package immediately.
