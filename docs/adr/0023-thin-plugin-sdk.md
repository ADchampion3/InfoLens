# Thin Plugin SDK

`@infolens/plugin-sdk` will standardize manifest typing, backend health startup, plugin runtime environment, OpenCLI JSON invocation, and workspace API-address helpers. It deliberately will not standardize source data, persistence, scheduling, components, or frontend framework so plugins retain full business and presentation ownership.
