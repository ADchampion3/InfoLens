# Persistent Plugin Navigation

The host renderer will use a persistent left-side plugin navigation rail and open the selected plugin's complete iframe workspace in the right main-content area. Plugin management and application settings live at the bottom of navigation; the host will not provide a content dashboard or cross-plugin feed.
