# Simplified Trusted Plugin Package

Infolens will use a simplified TractIt-style plugin package: a manifest, a backend entry, and a built Web workspace. The manifest declares identity, display metadata, entries, and OpenCLI command mapping; the host validates only what discovery and startup require, while omitting permissions, installation review, data generations, and governed upgrade transactions.
