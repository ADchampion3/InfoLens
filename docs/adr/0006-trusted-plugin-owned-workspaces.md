# Trusted Plugin-Owned Workspaces

Infolens will be a default-trust local plugin host: the host provides navigation, lifecycle status, and basic diagnostics, while every plugin owns its OpenCLI collection mapping, persistent data, refresh behavior, and complete main-content workspace. This replaces the proposed host-owned subscription workspace and unified reading projection because the MVP sources require materially different user experiences.
