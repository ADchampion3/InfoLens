# Copy Local Plugin Installations

Ordinary local-folder installation will copy a prebuilt plugin package into Infolens' fixed plugin directory, so an installed plugin remains stable when its original folder changes or moves. Plugin authors may use a separate explicit development-link workflow for rapid iteration, but links are not the user-facing installation default.
