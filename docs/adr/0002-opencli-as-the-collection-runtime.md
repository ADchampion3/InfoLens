# OpenCLI as the Collection Runtime

Infolens will be an independent product that uses OpenCLI as its collection and browser-execution runtime, without forking or copying OpenCLI's core. Infolens owns its subscriptions, retained information, refresh experience, and UI, while all collection mechanisms use OpenCLI's adapters and browser bridge.
