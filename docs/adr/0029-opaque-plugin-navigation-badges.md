# Opaque Plugin Navigation Badges

Plugins may return an optional short badge in their health response for the host navigation rail to display. The host treats the value as opaque metadata and does not define shared unread, task, or content semantics for it.
