# Fixed Directory and Local Plugin Installation

Infolens will scan its fixed project `plugins/` directory for plugin packages and support installing a prebuilt package selected from a local folder into that discovery path. The MVP will not use a plugin marketplace, Git installation, remote registry, or review workflow; installed local packages are trusted by default.
