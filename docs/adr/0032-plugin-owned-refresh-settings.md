# Plugin-Owned Refresh Settings

Bundled plugins will provide their own workspace settings for manual-only, disabled, and supported fixed-interval refresh policies. Each plugin persists and schedules that policy in its own SQLite store; the host displays only resulting lifecycle status and provides no global refresh settings page.
